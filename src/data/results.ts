import type { EventResult } from '../types/schedule';

/**
 * TARAS 2K26 Official Event Results Registry
 * Fresh production state — results populated dynamically via Firestore live results database.
 */
export const MOCK_RESULTS: EventResult[] = [];
